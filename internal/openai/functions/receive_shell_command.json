{
  "name": "receive_shell_command",
  "description": "Receive Shell Command and optionally check its completion",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": [
      "command",
      "working_directory"
    ],
    "properties": {
      "command": {
        "type": "string",
        "description": "The shell command to be executed"
      },
      "working_directory": {
        "type": "string",
        "description": "The directory in which to execute the command"
      },
      "task": {
        "type": "string",
        "description": "The overall task being performed (required for completion check)"
      },
      "current_step": {
        "type": "string",
        "description": "The current step being executed (required for completion check)"
      }
    },
    "additionalProperties": false
  }
}
