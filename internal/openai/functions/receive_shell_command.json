{
  "name": "receive_shell_command",
  "description": "Receive Shell Command",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": [
      "command",
      "timeout",
      "working_directory"
    ],
    "properties": {
      "command": {
        "type": "string",
        "description": "The shell command to be executed"
      },
      "timeout": {
        "type": "number",
        "description": "Time in seconds before the command times out"
      },
      "working_directory": {
        "type": "string",
        "description": "The directory in which to execute the command"
      }
    },
    "additionalProperties": false
  }
}
